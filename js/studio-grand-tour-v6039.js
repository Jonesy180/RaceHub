(()=>{const safe=s=>esc(String(s??''));window.rhConfirm=function({title,copy,detail='',safeguard='',confirmLabel='CONFIRM',cancelLabel='CANCEL',danger=false,severity='',onConfirm=''}){document.getElementById('rhConfirmOverlay')?.remove();const sev=severity||(danger?'destructive':'cyan');if(sev==='destructive'){const racing=/RESET RACING DATA/i.test(title);document.body.insertAdjacentHTML('beforeend',`<div id="rhConfirmOverlay" class="rhResetScreen6039 ${racing?'racing6039':'full6039'}"><div class="rhResetArtwork6039"></div><button class="rhResetBack6039" onclick="$('rhConfirmOverlay').remove()">‹ BACK</button>${racing?`<div class="rhResetTablet6039"><div class="warn6039">⚠ WARNING</div><h2>${safe(title)}</h2><p>${safe(copy)}</p>${safeguard?`<div class="keep6039">◇ ${safe(safeguard)}</div>`:''}</div>`:''}<div class="rhResetBottom6039"><strong>THIS ACTION CANNOT BE UNDONE.</strong><div class="rhResetActions6039"><button class="btn secondary" onclick="$('rhConfirmOverlay').remove()">${safe(cancelLabel)}</button><button class="btn dangerBtn" onclick="$('rhConfirmOverlay').remove();${onConfirm}">${safe(confirmLabel)}</button></div></div></div>`);return;}document.body.insertAdjacentHTML('beforeend',`<div id="rhConfirmOverlay" class="rhOverlay"><div class="rhModal rhConfirmModal rhConfirmFinal ${sev}"><button class="rhModalX" onclick="$('rhConfirmOverlay').remove()">×</button><div class="rhConfirmIcon">${sev==='purple'?'★':sev==='caution'?'!':'↻'}</div><h2>${safe(title)}</h2><p>${safe(copy)}</p>${detail?`<div class="rhConfirmDetail">${safe(detail)}</div>`:''}${safeguard?`<div class="rhSafeguard">◇ ${safe(safeguard)}</div>`:''}<div class="rhModalActions"><button class="btn secondary" onclick="$('rhConfirmOverlay').remove()">${safe(cancelLabel)}</button><button class="btn" onclick="$('rhConfirmOverlay').remove();${onConfirm}">${safe(confirmLabel)}</button></div></div></div>`);};})();