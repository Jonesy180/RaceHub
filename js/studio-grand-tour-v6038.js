/* RaceHub v6.0.38 — Grand Tour destructive reset full-screen Hubs artwork. */
(()=>{const safe=s=>esc(String(s??''));window.rhConfirm=function({title,copy,detail='',safeguard='',confirmLabel='CONFIRM',cancelLabel='CANCEL',danger=false,severity='',onConfirm=''}){document.getElementById('rhConfirmOverlay')?.remove();const sev=severity||(danger?'destructive':'cyan');if(sev==='destructive'){document.body.insertAdjacentHTML('beforeend',`<div id="rhConfirmOverlay" class="rhResetFull6038"><div class="rhResetArt6038" aria-hidden="true"></div><button class="rhResetBack6038" onclick="$('rhConfirmOverlay').remove()">‹ BACK</button><div class="rhResetLive6038"><div class="rhResetText6038"><h2>${safe(title)}</h2><p>${safe(copy)}</p>${detail?`<div class="rhConfirmDetail">${safe(detail)}</div>`:''}${safeguard?`<div class="rhSafeguard">◇ ${safe(safeguard)}</div>`:''}</div><div class="rhResetActions6038"><button class="btn secondary" onclick="$('rhConfirmOverlay').remove()">${safe(cancelLabel)}</button><button class="btn dangerBtn" onclick="$('rhConfirmOverlay').remove();${onConfirm}">${safe(confirmLabel)}</button></div></div></div>`);return;}document.body.insertAdjacentHTML('beforeend',`<div id="rhConfirmOverlay" class="rhOverlay"><div class="rhModal rhConfirmModal rhConfirmFinal ${sev}"><button class="rhModalX" onclick="$('rhConfirmOverlay').remove()">×</button><div class="rhConfirmIcon">${sev==='purple'?'★':sev==='caution'?'!':'↻'}</div><h2>${safe(title)}</h2><p>${safe(copy)}</p>${detail?`<div class="rhConfirmDetail">${safe(detail)}</div>`:''}${safeguard?`<div class="rhSafeguard">◇ ${safe(safeguard)}</div>`:''}<div class="rhModalActions"><button class="btn secondary" onclick="$('rhConfirmOverlay').remove()">${safe(cancelLabel)}</button><button class="btn" onclick="$('rhConfirmOverlay').remove();${onConfirm}">${safe(confirmLabel)}</button></div></div></div>`);};})();
